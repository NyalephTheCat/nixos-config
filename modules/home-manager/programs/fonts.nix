{ config, pkgs, lib, ... }:

{
  # Home Manager font configuration
  fonts.fontconfig.enable = true;
  
  # Additional user-specific fonts can be added here
  # System fonts are already available via NixOS configuration
}

